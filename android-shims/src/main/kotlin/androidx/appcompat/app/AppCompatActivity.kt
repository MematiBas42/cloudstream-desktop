package androidx.appcompat.app

import android.app.Activity

/**
 * AndroidX AppCompatActivity shim for desktop runtime.
 * Inherits [Activity] with identical lifecycle and event contracts.
 */
open class AppCompatActivity : Activity()
